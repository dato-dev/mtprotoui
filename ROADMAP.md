# Roadmap / TODO

Идеи по развитию MTProto UI. Чекбоксы — для отметки прогресса.

## ✅ Сделано
- [x] Поддержка типа прокси `tg-ws-proxy` (выбор при добавлении, порт, Fake TLS)
- [x] Аварийный сброс пароля админа через `ADMIN_PASSWORD_RESET`
- [x] QR-код proxy-ссылки в деталях сервера
- [x] Просмотр логов контейнера из UI
- [x] Редактирование сервера (имя, теги, SSH, порт) с авто-редеплоем при смене порта/хоста
- [x] Ретраи и таймауты деплоя (3 попытки × 6 мин на попытку, без зависания в «Развёртывание»)
- [x] Метрики контейнера через `docker stats` (CPU, RAM, сеть RX/TX, PIDs)
- [x] Массовые операции (проверить / удалить выбранные)
- [x] Поиск / фильтр по тегам / сортировка в таблице
- [x] Группы / теги серверов
- [x] Нейтральные формулировки шагов деплоя

## 🔒 Безопасность
- [x] **TOFU для SSH host keys.** Ключ хоста пиннится при первом подключении
      (таблица `known_hosts`), при несовпадении соединение отклоняется (MITM).
- [x] **Аудит-лог** действий: кто и когда добавил/изменил/пересоздал/удалил
      сервер (таблица `audit_log`, эндпоинт `GET /api/audit`, журнал в UI).
- [ ] **Многопользовательский режим + роли** (просмотр / управление).
- [ ] **Rate limiting** на `/api/auth/login` против перебора.

## ⚙️ Надёжность и эксплуатация
- [ ] **Тесты** бэкенда: `deploy.parseResult`, `crypto`, `auth`,
      генерация secret в скриптах.
- [ ] **Бэкап/экспорт конфигурации** серверов (с учётом шифрования secrets).

## 📡 Возможности прокси
- [ ] **Cloudflare Worker / fallback для tg-ws-proxy** — пробросить
      `TG_WS_PROXY_CF_WORKER` и `--cfproxy-*` (обход блокировок).
- [ ] **Кастомные DC IPs** для tg-ws-proxy (`TG_WS_PROXY_DC_IPS`).
- [ ] **Ротация SNI по расписанию** — периодически менять домен из whitelist.

## 📊 Наблюдаемость
- **Метрики трафика/подключений** — декомпозиция:
  - [x] Уровень 1: `docker stats` (CPU/RAM/сеть/PIDs) — универсально, по кнопке в деталях.
  - [ ] Уровень 2: число активных TCP-соединений через `ss -tn` на порту прокси
        (показать «клиентов сейчас»).
  - [ ] Уровень 3: метрики самого прокси — stats-endpoint mtg / парсинг `stats.py`
        у tg-ws-proxy (точное число клиентов, трафик по DC). Хрупко, зависит от
        версии образа.
  - [ ] Авто-обновление метрик (поллинг) и мини-графики вместо разовой выборки.
- [ ] **Уведомления при падении** (Telegram-бот / webhook) при уходе в офлайн.
- [ ] **История статусов / uptime-график**, а не только последняя проверка.

## 🎨 UX
- [x] **Drag-and-drop сортировка / ручной порядок** серверов (режим «Ручной
      порядок», порядок хранится в localStorage).
- [x] **Сохранение фильтров и сортировки** между сессиями (localStorage).

## 🧹 Тех-долг
- [x] Прогнать `gofmt -w` по неотформатированным файлам бэкенда.
- [ ] Линт фронтенда (ESLint) и CI-сборка.
